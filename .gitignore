# Swift / SPM
.build/
# xcodebuild SYMROOT output when TandemKit is built as a dependency (e.g. for a watchOS host)
build/
.swiftpm/
*.xcodeproj
Package.resolved
DerivedData/

# macOS
.DS_Store

# Oracle build artifacts (submodule builds its own jar; don't commit binaries)
*.jar
vendor/pumpx2-oracle/**/build/

# Local scratch/scan output (not part of the source tree)
/scratch_reads*.txt
